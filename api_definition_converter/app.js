var express = require('express');
var path = require('path');
var favicon = require('serve-favicon');
var logger = require('morgan');
var cookieParser = require('cookie-parser');
var bodyParser = require('body-parser');

var routes = require('./routes/index');
var users = require('./routes/users');
var raml2mashery = require('./routes/raml2mashery');
var swagger2mashery = require('./routes/swagger2mashery');
var swagger2iodocs = require('./routes/swagger2iodocs');
var wadl2mashery = require('./routes/wadl2mashery');
var wsdl2mashery = require('./routes/wsdl2mashery');
var copyapi = require('./routes/copyapi');

var app = express();

// view engine setup
app.set('views', path.join(__dirname, 'views'));
app.set('view engine', 'hbs');

// uncomment after placing your favicon in /public
app.use(logger('dev'));
app.use(bodyParser.json());
app.use(bodyParser.urlencoded({ extended: false }));

app.use(cookieParser());
app.use(express.static(path.join(__dirname, 'public')));

app.use('/', routes);
app.use('/users', users);
app.use('/raml2mashery', raml2mashery);
app.use('/swagger2mashery', swagger2mashery);
app.use('/swagger2iodocs', swagger2iodocs);
app.use('/wadl2mashery', wadl2mashery);
app.use('/wsdl2mashery', wsdl2mashery);
app.use('/copyapi', copyapi);

// catch 404 and forward to error handler
app.use(function(req, res, next) {
  var err = new Error('Not Found');
  err.status = 404;
  next(err);
});

// error handlers

// development error handler
// will print stacktrace
if (app.get('env') === 'development') {
  app.use(function(err, req, res, next) {
    res.status(err.status || 500);
    res.render('error', {
      message: err.message,
      error: err
    });
  });
}

// production error handler
// no stacktraces leaked to user
app.use(function(err, req, res, next) {
  res.status(err.status || 500);
  res.render('error', {
    message: err.message,
    error: {}
  });
});

module.exports = app;
